name: test
channels:
  - conda-forge
  - defaults
dependencies:
  # obspy
  - decorator
  - lxml
  - matplotlib
  - numpy
  - scipy
  - requests
  - setuptools
  # see #3258
  - sqlalchemy < 2.0
  # tests
  - pytest
  - pytest-cov
  - pytest-json-report >= 1.4
